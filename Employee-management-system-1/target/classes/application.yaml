server:
  error:
    include-stacktrace: always
spring:
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
  datasource:
    url: jdbc:mysql://localhost:3308/ersity?createDatabaseIfNotExist=true
    username: root
